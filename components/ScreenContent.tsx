import { View, Text } from 'react-native';

type ScreenContentProps = {};

export const ScreenContent = ({}: ScreenContentProps) => {
  return (
    <View>
      <Text>this is ScreenContent</Text>
    </View>
  );
};
