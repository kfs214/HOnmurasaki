[WIP]

### 🧪 Running Tests

This project uses [Playwright](https://playwright.dev/) for testing.

Before running tests, make sure to install the required browsers:

```bash
npx playwright install
```
